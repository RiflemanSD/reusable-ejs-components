<%
  // Alert Box Component - Show alerts, warnings, success, error messages
  // Required: message, type
  // Optional: icon, dismissible
  
  var alertType = type || 'info';
  var alertClass = alertType === 'success' ? 'alert-success' : 
                   alertType === 'warning' ? 'alert-warning' : 
                   alertType === 'error' ? 'alert-error' : 
                   'alert-info';
  var alertIcon = typeof icon !== 'undefined' ? icon : null;
  var isDismissible = typeof dismissible !== 'undefined' ? dismissible : false;
  
  // Default icons based on type
  if (!alertIcon) {
    if (alertType === 'success') {
      alertIcon = '<svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>';
    } else if (alertType === 'warning') {
      alertIcon = '<svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" /></svg>';
    } else if (alertType === 'error') {
      alertIcon = '<svg xmlns="http://www.w3.org/2000/svg" class="stroke-current shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>';
    } else {
      alertIcon = '<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-current shrink-0 w-6 h-6"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>';
    }
  }
%>

<div class="alert <%= alertClass %> shadow-lg" <% if (isDismissible) { %>id="alert-<%= Math.random().toString(36).substr(2, 9) %>"<% } %>>
  <% if (alertIcon) { %>
    <%- alertIcon %>
  <% } %>
  <span><%- message %></span>
  <% if (isDismissible) { %>
    <button class="btn btn-sm btn-ghost" onclick="this.closest('.alert').style.display='none'">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
  <% } %>
</div>
